# Machine-Readable Publication Policy

Human pages and machine-readable records MUST resolve to the same IDs and current release. Published JSON MUST validate against the versioned schemas, retain stable identifiers, expose the applicable standard and protocol versions, and avoid long copyrighted quotations.

CSV and API representations MAY flatten records but MUST NOT erase provenance, knowledge state, limitations, review dates, or change-event identity.

